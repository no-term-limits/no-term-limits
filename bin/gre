#!/usr/bin/env bash

set -eo pipefail

# HELP: grep recursively in all directories minus .svn and .git. run "cac gre" for full exclusion list

gns --exclude={*.log,tags} --exclude-dir={target,coverage,doc,build} "${@}"
