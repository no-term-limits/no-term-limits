#!/usr/bin/env bash

set -eo pipefail

# HELP:

gem_name="$1"
group_name="$2"

if [[ -f Gemfile ]]; then
  if sed -E "/group .*:${group_name}\b/,/^end/!d" Gemfile | grep -q " *gem ['\"]$gem_name['\"]" ; then
    exit 0
  fi
fi

exit 1
