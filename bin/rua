#!/usr/bin/env bash

set -eo pipefail

# HELP: rubocop --autocorrect

if [[ -f Gemfile ]] && grep -q rubocop Gemfile; then
  bundle exec rubocop --auto-correct "$@"
else
  rubocop --auto-correct "$@"
fi
