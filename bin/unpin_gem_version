#!/usr/bin/env bash

set -eo pipefail

# HELP:

gem_name="$1"
if [[ -z "$gem_name" ]]; then
  >&2 echo 'ERROR: a gem name must be specified'
  exit 1
fi

perl -pi -e "s/(gem [\"']${gem_name}[\"']).*/\1/" Gemfile
