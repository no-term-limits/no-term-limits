#!/usr/bin/env bash

# designed to be run as the very first (and only) setup script

function error_handler() {
  echo "Exited with BAD EXIT CODE '${2}' in ${0} script at line: ${1}."
  exit "$2"
}

trap 'error_handler ${LINENO} $?' ERR

set -o errexit
set -o errtrace
set -o pipefail

export NO_TERM_LIMITS_RCUP_FORCE=true
export NO_TERM_LIMITS_INSTALL_ASDF_LANGUAGES=false

./setup/provision_no_term_limits
ls -al ~
echo 'here is zshrc'
cat ~/.zshrc
./setup/check_dotfiles setup/expected_dotfiles.txt
